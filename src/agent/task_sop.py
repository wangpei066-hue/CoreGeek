"""供平台 LLM 复用的精简部署修复规则；具体参数始终来自当前任务。"""

DEPLOYMENT_SOP = r'''
部署修复规则：
1. 先从当前任务和 spec.md 提取工作区、目标路径、精确权限、配置物理行号/内容、验证命令和提交格式。
2. 工作区必须已确认且存在；一次 execute 完成创建允许创建的目录/文件、配置修改、chmod、回读校验和最终 check。用 set -eu；不要修改 check 绕过验证，不执行无关服务。
3. 配置按物理行修改并保留其他内容；缺失配置是否创建必须以题目为准。权限使用 chmod 755 等精确值，不使用 chmod -R。
4. 沙箱仅保证 sh 和常见 Linux/POSIX 命令，不能依赖 Python。可使用 mkdir、chmod、sed、awk、grep、test、stat、find、tr 等；复杂文本修改应先备份/校验，避免行号漂移。
5. 首次 check 失败后只根据真实输出修复；最终验证必须获得完整成功证据。成功后下一轮立即 submit，并提交真实 TOKEN 或题目要求的答案。
'''
