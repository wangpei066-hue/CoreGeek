# task_solver 模拟迭代记录

## 迭代 0（2026-09-16）

- 版本：`20260915-solver3`
- 6任务上限：14轮
- 结果：受限 sandbox `0/6`（本地 API 端口绑定和外部 LLM 连接被拒绝）。
- 升级权限基线：`task_1_alpha.md` 成功，9轮；`task_1_beijing.md` 成功，8轮；后续 API 任务在模型只返回默认第一页摘要后未能在14轮内完成。详见 `iteration-0-escalated.json`。

## 迭代 1（2026-09-16）

- 版本：`20260916-solver4`
- 修改：API SOP 强制显式 `offset/limit`、一次命令循环分页、去重并只输出最终 JSON；禁止样本/字段探查。
- 修改：允许求解器接受带有 `offset` 与 `limit` 证据的一次性最终统计摘要，避免因模型不打印原始记录而重复询问。
- 回归：`py_compile` 通过；完整升级权限回归生成 `iteration-1.json`，运行在 API 任务阶段因调用超时中止，报告尚未完成，需下一轮单任务复跑确认。
- 安全：测试报告不包含用户 API key；密钥仅通过进程环境变量注入。

## 迭代 2（2026-09-16）

- 版本：`20260916-solver5`
- 针对南京任务补充 `era_order=null` 时的明确年代比较规则，并扩展摘要证据兼容目标字段变体；待完整6任务回归确认。

## 迭代 3（2026-09-16）

- 版本：待提交
- 修改：在 `summarize_heritage_records` 中加入有证据的中国历史年代标签排序，解决模拟数据 `era_order=null` 时的 oldest 选择浪费轮次。
- 验证：`py_compile` 通过；南京单任务 solver5 仍为14轮超时，说明还需绕过模型对 API_DOCS 的重复探查。

## 迭代 4（2026-09-16）

- 版本：待提交
- 修改：读取 API 任务简报后自动复用已验证遗产 API 合同，跳过过时 `API_DOCS.md` 探查；保留 `location`、Bearer 和分页契约。
- 目标：API 任务在读取简报后直接进入确定性分页，减少至少两轮 LLM 往返。
- 修正：发现通用读文档转移覆盖了自动 API 阶段，已在后续提交中修复。

## 迭代 5（2026-09-16）

- 版本：待提交
- 修改：防止通用 `read` 状态覆盖 API 自动分页状态，避免重复读取/重新询问。

## 迭代 6（2026-09-16）

- 验证：南京单任务仍未在14轮内提交；第2轮自动 API 请求出现 `HTTPSTATUS:000`，第6轮 LLM 已产生完整正确统计，但状态机未完成摘要转提交。
- 结论：下一轮优先修复 API 服务启动竞态重试及摘要完成状态传递，再进行完整6任务回归。

## 迭代 7（2026-09-16）

- 版本：`20260916-solver6`（本轮提交前的工作树）
- 修改：API 传输 `HTTPSTATUS:000` 最多确定性重试2次；提交前从带分页证据的历史执行结果恢复完整摘要状态。
- 完整6任务回归：`6/6` 成功，全部不超过14轮：alpha 9、北京 13、南京 8、beta 9、成都 14、gamma 13。
- 回归报告保存在本机临时路径 `/tmp/task-suite-final.json`；仓库仅提交摘要，避免推送原始提示和轨迹。

## 真实网络日志诊断与修复（2026-09-16）

- `true_network/nanjing&beta.log` 显示 beta 已通过 `./check` 并拿到 TOKEN，但任务切换后停在 `wait_submit`，没有再次发出 `submitAnswer`。
- 修复：`submit` 与 `wait_submit` 均进入幂等重发分支，直到收到平台确认；已确认任务不再重发。

## 真实环境无限调度修复（2026-09-16）

- 按真实平台约束移除 solver 内部 LLM 调用次数上限和 `budget_insufficient -> exhausted` 终止路径。
- 旧会话若已落入 `exhausted`，恢复时自动转为 `submit`（已有答案）或 `ask`（未有答案）。
- 本地模拟仍由 `LocalTaskDriver --max-rounds 14` 控制测试边界。
