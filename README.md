# 《未来战争》Python V1

## 多人协作开发

团队成员从 [开发与 GitHub 协作准则](CONTRIBUTING.md) 开始：任务认领、分支命名、PR 审查、目录职责、测试及发布流程均以该文档为准。仓库提供 Issue/PR 模板及 Python CI；远程分支保护和成员权限需要管理员按文档配置。

保留用户确认的官方 SDK 约定：`POST /` 接收 JSON 对象，由 `GameServer` 生成响应对象。V1 已接入真实策略：解析请求为结构化状态（`MatchState`）→ `V1Strategy` 按白天/夜晚生成指令 → `BasicActionValidator` 本地校验后返回 `roleCommandMap`。

## V1 范围

**已实现**：
- 状态解析（P1）：接口文档 1.1–1.7 全部字段映射为结构化实体。
- 8 方向等代价多目标 BFS 寻路（同时保留 A* 单目标工具），碰撞避让（含同回合己方多角色目标格互斥）。
- 第二天起的白天经济循环：按报价和运输成本选矿，达到变现阈值后专程卖矿；只保留少量建墙石料，返程时间不足时提前回防。首日三座火箭齐后，矿石可变现约130金（或任务拿到相近金币）时卖矿买一张武器升级券。官方消息矿价日程写入 `officialPlan` 日志，当前不改采矿。
- 建造选址：因精确可建造区坐标未核实，采用"基地周围环形扩展试探 + 按建筑类型记录失败并限时避让"的经验性策略，而非猜测蓝/黄区范围；该黑名单与待建造目标落盘到 `state/build_memory.json`，服务重启可恢复，不用每次从头试探。
- 夜晚战斗：角色贴身操控射程内武器攻击，按 BOSS>大型>中型>小型优先级选目标，同优先级内优先集火低血量；火箭冷却中不发起攻击；加特林/火箭按武器等级填充目标位置数。
- 自救是最低优先级兜底：角色没有施工、经济、回防或操炮动作时，若低于半血且持有 Medicine 才使用；不因自疗取消本回合攻击。满血数值按任务书4.5.1/4.5.2 表格。
- 围墙维修与武器/围墙/基地升级券：空闲角色（工人或开拓者，`buy`/`use`/`sell` 是全角色可用动作）机会性执行"买道具→走到目标建筑一格内→use"两段式任务，优先级为 补墙（缺口重建）> 武器升级券 > 修墙 > 围墙升级券 > 基地升级；同一目标建筑不会被两个角色重复分配。该任务队列（`worker_item_jobs`）与建造记忆一起落盘到 `state/build_memory.json`。
- 本地指令合法性校验（`BasicActionValidator`），拦截缺字段等明显非法指令；贩卖时只统计矿石类物品，不会把背包里的升级券/维修包/药品当矿石卖掉。

- 自进化任务已接入：pioneer 仅在白天安全窗口前往任务点并发出 `acceptTask`；任务让位于回防和变现。收到任务后经平台沙盒读文档、调用 LLM 解题并提交；夜间暂停解题器输出，三名存活角色参与武器分配。
- 推理类（官方消息）：启发式抽出停工/涨价日程，写入 `news_memory.officialPlan` 并打 `official_plan` 日志；可选矿价 LLM 覆盖同一份 JSON。当前**不**据此改工人采矿/卖矿。
- 长上下文（民间传闻）：跨日累积后**直接**申请宝藏 LLM，写入 `news_memory.folkPlan` 并打 `folk_plan` 日志；不做正则启发式。当前**不**据此指挥开拓者买物/`summonTreasure`。日志搜 `official_plan` / `folk_plan`。

**尚未实现**：

- 眩晕法宝。范围炸弹与机器人召唤令已接入有预算限制的战术消费，见下文。
- 角色阵亡复活后的状态衔接、换边重置。

自进化任务解题链路已通过本地协议测试；实际得分、平台 LLM 输出和沙盒路径仍需官方对局验证。

## 项目结构与迁移

```text
main3.py                 程序入口（简洁转发，实际逻辑在 src/agent/）
src/agent/
  ├── __init__.py      导出公开接口
  ├── protocol.py      数据结构和状态定义
  ├── grid.py          网格算法和寻路
  ├── brain.py         策略和决策逻辑
  └── server.py        HTTP服务器和持久化
requirements.txt        Python 依赖（Flask, etc.）
README.md              启动、迁移、测试说明
.github/               GitHub Workflow 和 PR 模板
docs/                  赛题文档和审计
tests/                 本地测试用例（运行时不需要）
logs/                  启动时自动创建，请求/响应日志
state/                 启动时自动创建，跨回合学习记忆
```

**运行时最小依赖**：只需 `main3.py`、`src/agent/`、`requirements.txt` 三个部分。迁移运行：
1. 复制 `main3.py`、`src/agent/` 目录、`requirements.txt` 到可写目录
2. 执行 `pip install -r requirements.txt`
3. 执行 `python main3.py 6666` 启动服务

`logs/` 和 `state/` 会在运行时自动创建。无需复制测试、文档、日志或状态。

**架构说明**：代码采用标准 Python 包结构，核心逻辑分解为四个专职模块：
- `protocol.py`：数据协议与状态定义
- `grid.py`：网格算法（A* 寻路、碰撞检测）
- `brain.py`：游戏策略与决策（V1Strategy）
- `server.py`：HTTP 服务器与持久化

策略入口为 `src/agent/brain.py`，首日与防线布局在 `opening.py`，主动变现在 `economy.py`，范围炸弹与召唤令在 `tactics.py`。这些模块均通过 `main3.py` 的服务入口调用，测试文件不参与运行策略。

## 启动与测试（PowerShell）

在项目根目录执行：

```powershell
python -m pip install -r requirements.txt
python main3.py 6666
```

官方比赛环境为 Python 3.11.10（《编译运行环境说明》仅确认版本号，未说明依赖包版本或其他限制）。开发机当前已安装 Python 3.12.7、Flask 3.0.3；代码未使用 3.12 专属语法，与 3.11.10 兼容，但建议提交前在 3.11.10 环境下复测一次。端口已被占用时请使用其他端口，不要停止原有服务。

另开 PowerShell，使用文件发送 JSON，避免 Windows 命令行的引号转义差异：

```powershell
curl.exe -i --noproxy "*" -X POST http://127.0.0.1:6666/ -H "Content-Type: application/json" --data-binary "@tests/fixtures/valid_request.json"
```

预期 HTTP 200，JSON 包含 roleCommandMap、prompt、executeCmd，键顺序不作要求。fixture 是人工联通输入，不是官方对局请求。

```powershell
python -m unittest discover -s tests -v
```

测试包含临时目录和独立子进程的真实 HTTP 200→400→200 检查，自动关闭自己启动的子进程，不操作已有服务。测试结果见 `results/baseline/python-tests.log`。

## 实际行为与边界

- `logs/`、`state/` 相对 main3.py 所在目录定位，不依赖工作目录。
- 每份合法 JSON 对象以 UTF-8 格式保存为 `logs/request_000001.json` 等，重启后跳过已有文件。保存的是格式化后的对象，不是原始 HTTP 字节。
- 非法 JSON、数组、null、标量或不符合 JSON Content-Type 的请求返回 HTTP 400，且不记录为合法请求。
- 日志或 callback 异常返回 HTTP 500 并记录异常到 stderr，服务继续处理后续请求。
- `state/build_memory.json`：`MatchState` 的跨回合学习记忆（建造失败黑名单、待建造目标、维修/升级任务队列、上一次发送的指令）落盘于此，`callback` 每次请求前读（进程内只读一次）、处理后写；写入用临时文件+原子替换防止写坏；没有任何值得记忆的内容时（如纯连通性测试请求）不落盘，`state/` 保持空目录。
- `src/agent/protocol.py` 中 `GameState` 为抽象接口；`MatchState(GameState)` 实现 `update()` 将请求 JSON 解析为结构化字段（`Pos`/`Zone`/`MapInfo`/`Role`/`PlayerTask`/`TeamOur`/`TeamEnemy`/`RobotRole`/`WorldNews`/`ShopItem`/`ErrorInfo`），每回合全量重建。`src/agent/brain.py` 中 `Strategy` 由 `V1Strategy` 实现，`ActionValidator` 由 `BasicActionValidator` 实现，均已接入 `src/agent/server.py` 的 `GameServer`。`TaskSession` 仍是未实现抽象接口（自进化任务由 PioneerTaskSolver 实现）。
- 当前使用 Flask 默认 JSON 解析，不宣称实现额外的重复键、NaN、请求大小、响应总时限等严格校验。
- `roleCommandMap` 里只有 V1 主动决定行动的角色才会出现在 map 中，其余角色本回合不下发指令（待机）；判题器是否接受"角色不出现在 map 中"为待机尚未经真实对局验证。
- 已通过 `tests/fixtures/sample_match_state.json`（本地构造、非官方样本）人工联通验证 V1 会针对真实结构的请求生成非空 `roleCommandMap`；尚未接入官方判题器实测决策效果。

不继续开发 C++、WSL 或 CMake。下一步：接上官方判题器验证 V1 白天经济/夜晚战斗闭环，再据反馈校准建造区、roundNo 起始值等假设，随后规划任务系统（V2）。


## 2026-09-11 正确性修正

- 基地左上角坐标向右、向下展开2×2占地；交互寻路搜索全部邻接空格。
- 白天决策使用回合内金币余额及武器建造名额，保留输入快照中的真实金币。
- 失败建造按武器/围墙分别避让30回合，到期重试；30为可调经验参数，不代表永久非法区域。
- 道具任务在购买前复核金币，发送use后等待反馈；失败且道具仍在时继续任务。
- 持久化保存对局上下文与回合；阵营/地图/基地变化或回合回退时清空历史计划。
- HTTP请求在单进程内串行更新状态；本地校验新增动作白名单、坐标和数量检查，并记录过滤原因。
- 源码启动：`python main3.py 6666`。安装后的`agent-server 6666 --data-dir <目录>`可显式指定运行目录。
- 回归测试：`python -m unittest discover -s tests`。真实HTTP测试使用临时数据目录。

边界：没有官方对局ID时，无法识别所有“同地图、同阵营、同回合号”的新对局；多进程部署不共享内存锁。昼夜起始回合、建造区与策略收益仍待判题器实测。


## 本地决策日志

日志 schema v2 增加 `diagnostics`，本地 JSON、可读 TXT 与平台分类 stderr 均可查看。平台下载请先读 [`docs/logging.md`](docs/logging.md)。

- 进程启动时 stderr 第一行是 `BUILD_INFO` / `event=commit`（整次执行只打一次），字段 `commit` 为 git HEAD；找不到 `.git` 时为 `unknown`。
- `source_version`：策略及诊断源文件指纹，配合 UTC 时间、队伍、回合和请求序号。它不是 Git 提交号。
- `primary/outer`：分别列出规划数、已建数、缺口坐标、待升级和待维修位置；第一层侧墙延伸至短射程武器列，确保三座新建武器都位于正面与侧面墙体之后；`outer_unlocked` 显示二层准入状态。
- `actors`：矿石数量、当前报价估值、未知报价品类、背包容量、卖矿承诺、到小贩和分配武器的寻路步数。分配为诊断估算，实际动作以 `roles.command` 为准；步数 `null` 表示不可达或无目标，不代表零步。
- `weapons`：位置、等级、血量、射程、冷却、附近操作者、射程内机器人和实际攻击指令。
- `weapon_upgrade_deadline`：第三夜（当前0起算假设为回合330）前仍待升级的武器、按实时商店价格计算的总需求、当前金币、资金缺口和剩余回合；进入第三个白天仍未完成会产生 `WEAPON_UPGRADE_DEADLINE`。
- `alerts`：优先排查 `PRIMARY_GAPS`（一层缺口）、`NIGHT_UNSTATIONED`（夜间未就位）、`ORE_AT_RISK`（携矿风险）、`WEAPON_NOT_FIRING`（可射击但未攻击）、`MOVE_NO_PROGRESS`（移动后位置没变）、`ACTION_FAILED`（系统报告失败）、`ROLE_DIED`（观察到死亡，附前一快照背包）。告警是复盘线索，不把未开火一律判为错误。
- `gold_delta` 仅是同局快照金币净变化，不能直接当作卖矿收入；首次请求或重启不计算跨快照变化，并输出地图和商店报价用于复现。

本地日志文件写入和控制台输出独立处理，一路失败不阻断另一路或比赛响应。日志计算不修改策略状态。缺少官方回合起点确认时仍沿用0起算；不从日志推断尚未验证的胜率、收益或死亡原因。

服务每次成功完成策略决策后，在 `logs/` 下新增与请求、响应编号一致的两份文件：

- `decision_000001.txt`：中文可读摘要，用于快速查看为什么不建墙、为什么待机。
- `decision_000001.json`：结构化记录，包含局面、角色动作、真实决策分支、系统反馈和快照差异。

可在 PowerShell 实时查看某份日志：`Get-Content logs/decision_000001.txt -Encoding UTF8`。
编号对应请求顺序，不一定等于游戏回合；游戏回合位于文件内部的 `round` 字段。

记录内容包括金币、基地血量、武器/围墙/机器人数量、角色位置和背包、建造条件、资金不足、
路径不可达、维修升级任务、实际下发动作、本地校验拦截以及上一条指令的执行反馈。
武器攻击会关联到操控者，避免把正在操炮的角色误记为闲置。

日志中的原因由实际执行分支记录；系统只返回失败时标注“具体原因未知”，全局错误不会擅自归因到某个角色。
快照差异只表示观测变化，不证明因果关系；进程重启后首个请求没有上一快照可比较。
昼夜标签沿用当前策略的0起算假设。自定义策略如果未提供分支事件，日志会明确说明原因未记录。

决策日志不写入比赛响应，也不持久化到策略记忆。诊断文件写入失败只记录服务端错误，
不会因此将正常响应改成500；原有请求、响应和状态文件的错误处理保持不变。

平台 stderr 每行一条 JSON，带中文 `title`，按 `marker` 分类（详见 [`docs/logging.md`](docs/logging.md)）：

- `STRATEGY_DECISION`：每回合总览（金币、武器/墙数量、告警码、角色指令）。
- `BUILD_WEAPON`：已建炮、升级资金、本回合建造/买券/开火。
- `BUILD_WALL`：一层/二层进度与本回合砌墙、采石。
- `PIONEER_TASK`：每回合自进化动作；接取/解题时另有 solver 细节。空闲不刷屏。
- `ECONOMY`：仅采矿/卖矿有活动时。
- `NEWS_INFER`：`official_plan` / `folk_plan`（说明见 [`docs/news.md`](docs/news.md)）。

平台只显示控制台时，按 marker 搜索即可。完整快照仍在容器 `decision_*.json`。这些日志不参与比赛响应。


## 第一日防守阶段策略

第一天（沿用roundNo从0起算的0–69回合假设）使用 `src/agent/opening.py`：

1. 两名工人先架好三座火箭炮，再采矿/做任务凑到约130金买一张武器升级券，升最前面那座火箭。卖矿时间点按剩余回合估算：必须还能买券并修完迎敌7–8段墙再回防；来不及就先修墙。金币已够买券时优先买一张再修墙。晚上保证三人三炮。
2. 工人缺少建墙材料时前往石矿，每批最多6块。三座火箭齐且尚未完成首日升级前，按报价采矿以便变现买券。
3. 按基地实际坐标确定迎敌方向，优先完整内层正面墙，再建外层（中央留一格），再补前半部侧翼。第一天墙目标约8段，第二天补到11–12段；后方开放；已建数量以快照为准。
4. 开拓者退到墙线内侧避让；封墙前检查返回武器、小贩、商店、石矿及有效任务点的既有通路。己方寻路避开外层诱敌口，从后方通行。
5. 按返程路径长度加3回合余量提前就位。夜间一人一炮，冷却时不抢其他人的武器；优先BOSS、大型、中型、小型，同级优先低血量。

决策日志记录首日阶段、墙线缺口、施工受阻与武器分配。第二天再升一门火箭，同时把墙修到11–12段，之后依次把火箭升到三级；同样主动卖矿、战术消费、维修升级，并按返程距离提前回防。

几何墙线和墙内武器格是候选布局，尚无官方合法建造区坐标，失败会按已有冷却机制换格尝试。
如果候选格不合法、石矿过远或行动回合不足，不能保证第一夜前完成一圈；日志不会把失败或待建格算作完成。
本地70回合合成地图回归测试验证三座火箭、迎敌约8段墙和三人就位，但不替代官方对局生存验证。

## 主动变现与战术消费

- 第二天起，满足任一条件就专程卖矿：可卖矿石按当前报价估值至少10金币、可卖数量至少6个、背包占用至少20%、角色血量低于60%、基地血量低于80%，或进入白天第20回合。已在小贩旁时也可出售。卖矿目标持续到可卖矿石清空，并跨服务重启保存；往返及出售时间不足则暂停外出。基地低于70%血量时，施工石料预留降至最多1块。
- 第二天工人优先级：到点回防 → 未达当日升级节奏则筹资升火箭 → 按阶段补墙（次日11–12段） → 为防守变现、补齐武器 → 维修升级 → 第二层墙 → 其他战术/经济行动 → 自救。卖矿与任务用于支撑防守资金，不以扩建第二层取代第一层加固。
- 第二层准入门槛：第一层正面及侧翼全部存在、至少二级且各段血量至少80%，三座存活武器均至少二级。这里的二级是当前策略参数；第一层再次破损或缺失时立即暂停第二层施工，旧的外墙施工目标也会失效。已有第二层不拆除。
- 白天第50回合进入回防期；路程加8回合余量已接近天黑，或基地周围达到高压力时更早回防。首日工人仍按施工计划的实际返程距离集合。开拓者任务不能覆盖回防，接任务前检查去程、任务超时预算及返程余量；没有武器也先回基地。
- 新建武器编制为三座火箭炮。三座都尽量贴内墙后1格，分列基地上下两侧先手消耗：第一座占迎敌中线，其余错开两翼，随出生侧镜像。第一层上下侧墙仍包住前列武器；后方保留己方通道。候选被占或非法时尝试同层其他格及邻层。已有武器无法直接移动；当前不自动覆盖或重建已有升级武器。
- 三座火箭尽快升到三级，同一时刻只锁一门：优先迎敌最前、当前等级最低的那座。第一天升最前一门（1→2），第二天再升一门，墙够12段后再依次2→3。每个未满级武器会分配独立升级任务，金币不足也保留目标并继续筹资；尚未购买升级券的城墙/基地任务会让位。按任务书基础价 1→2 合计300、再加 2→3 合计450，实际使用 `weaponShopList` 实时报价。
- 报价来自快照 `vendorShopList`，未知报价不虚报收益。矿点排序按一批10次采集的价格、去程寻路和返售距离估算；这不是实际对局收益预测。满背包停止采集。
- 道具任务优先级：补墙（被摧毁后重建）> 购买/使用武器升级券 > 修墙 > 购买/使用墙体升级券 > 升级基地。已经持有对应道具时无需再次满足购入金币门槛，继续用完。未购入的修墙或升墙任务会让位于武器升级。
- 召唤令购买门槛：基地血量至少70%、至少3座武器和6段墙、无高防守压力，且三座武器均至少二级。单次最多花当前余额25%，并至少保留100金币防守预算；选择预算内最高等级，团队有未用召唤令时不重复购入。仅在仍有后续夜晚的白天使用，每天最多发出10次使用尝试（失败也保守计数，跨重启保存）。
- 基地7格内至少4个敌方目标机器人，或基地低于60%血量且附近有机器人，视为高压力。法宝（范围炸弹、眩晕法宝）只在高压力下使用或购买：炸弹优先打密集目标，否则眩晕；没有库存则尝试购买。平时不消耗法宝。
- 额外记录曾经建好的正面墙位并跨重启保存；墙消失且敌人进入基地7格内，即触发 `front_breached`。此时单个敌人也可触发炸弹，不再等待密集目标；没有炸弹时可用已持有的维修包修复相邻存活墙段，或在商店相邻格花金币购买炸弹。夜间守卫不离炮远行购物，不能远程购买，也不能用维修包复活已消失的墙。
- 炸弹按任务书为100金币、3×3范围、每个机器人100伤害；眩晕法宝100金币、3×3晕5回合。二者都不能全图清除。双层墙外层中央小口是实验布局；机器人可能直接攻击城墙，分流效果、合法建造区和真实生存率必须通过官方回放检验。

专项回归测试位于 `tests/test_economy_tactics.py`。Windows 原生进程测试直接运行 `main3.py`；Linux 运行 `run.sh`。依赖 POSIX `sh` 的沙盒集成测试在缺少该程序时明确跳过，需在比赛 Linux 环境补跑。

## 自进化任务与平台下载日志

上传 `main3.py`、`run.sh`、`requirements.txt` 和完整 `src/`，使用 `bash run.sh <port>` 启动。
pioneer 按距离选择 `teamOur.playerTasks` 中 `isValid=true` 且 `coldDownRounds=0` 的自进化任务点，在周围一格内发送无额外参数的 `acceptTask`。正在执行任务时保持位置，必要时原地使用生命药剂；任务结束后按系统最新任务点状态重新选择，不自行推算冷却。

解题由 `src/agent/task_solver.py` 的跨回合状态机实现：

1. 任务类型不限，`taskKind` 仅提供工作区/API 等启发式线索，不限制 LLM 的解法。识别明确标注的工作区路径，或运维描述中唯一的绝对目录（支持 `ws\_3` 这类 Markdown 转义），并跨回合保存；无法识别时由 LLM 根据原文和文档确认。正则提取任务描述中的 `.md` 路径，支持反引号、引号、中文引号和普通路径；有空格的路径应在题目中用引号包围。
2. 通过 `executeCmd` 在平台沙盒运行基于 `sh` 和 Linux/POSIX 原生命令的脚本：指定工作区时先进入该目录读取文件，文件不存在就返回错误，不搜索其他项目。未指定工作区时，优先读取指定路径；相对路径不存在时，先搜索当前目录，再在限时范围内搜索文件系统。同名文件有歧义时返回候选路径给 LLM，不任意选择。
3. 下一回合解析 `lastCmdResult`，校验请求关联标识。文档每页 6000 字符，继续分页直到读完；每份文档自动读取上限 60000 字符，超出部分提示 LLM 按需读取。
4. 将任务原文、已读取文档、工具结果构造成响应顶层 `prompt`，使用比赛接口的 LLM，无需额外 API Key。任务期间的 LLM 调用按接口文档 1.7 豁免每日额度；代码另设每任务最多 12 次调用，防止无限重试。
5. 下一回合解析 `llmResp`。要求 LLM 返回 `{"action":"execute","command":"..."}` 或 `{"action":"submit","taskAnswer":"..."}`。前者通过平台沙盒执行，再把真实输出交给 LLM；另支持 `{"action":"read","path":"说明文件路径"}` 读取动态发现的任意文本说明并自动分页。`read` 和 `execute` 均支持附加 `"workspace":"目录"`，为本次及后续命令设置工作目录，避免跨回合丢失 `cd`。运维任务要求实际修改并验证，API 任务要求按文档调用并检查真实响应；后者生成 pioneer 的 `submitAnswer`。如最终答案本身是 JSON，仍按比赛协议序列化为 `taskAnswer` 字符串。

为减少一次领取两个任务时的时间消耗，prompt 要求信息齐全后用一次 `execute` 完成所有必要操作和验证，已有充分依据则直接提交。涉及工作区或配置时，运行 `check` 等最终验证前须确认任务目录正确且存在、必要修改已保存并回读确认；任一前置步骤失败就停止，避免未修改配置便运行检查。该要求由 prompt 指导 LLM 生成脚本，执行器不会静态拦截任意命令中的 `check`；单次命令仍有 10 秒限制，信息不足或验证失败时仍需补充交互。
工作区未明确时，运维任务的相对文档路径先交给 LLM 根据实际环境确认，避免全盘搜索误读其他项目。未提供文件名时先探索目录，文档引用新的目录或文件时可继续读取并更新工作区。文件名、验证命令、成功条件和提交格式均由当前任务及实际文档决定。

以下仅为示例，不是固定规则：对于部署修复类任务，例如指定 `ws_3/` 下的 `spec.md`：先在工作区读取规范，再执行修复和 `./check`；提示 LLM 在检查全部通过并真实输出 `TOKEN:xxx` 后，将实际 token 序列化为 `{"token":"xxx"}` 提交。验证失败时继续修复；本地测试使用模拟 LLM 响应验证跨回合流程。

6. 系统明确反馈答案错误或提交动作非法时，将反馈交给 LLM 修正。任务结束、队伍变化或回合倒退时清理会话；会话保存在 `state/task_session.json`，支持进程重启恢复。缺失工具执行反馈时交给 LLM判断，不自动重复执行未知副作用的命令。

任务日志标记为 `PIONEER_TASK`。读取文件和执行工具时，平台沙盒输出包含 `requestId`、`event`、正文或结果的 JSON，下一回合进入 `lastCmdResult`。未使用沙盒解题的任务回合补充 `printf` 诊断，包含 `solverStage`、角色反馈、任务原文分片。诊断不会覆盖读文件或工具命令。程序 stderr 在接取/解题回合记录任务、阶段、`llmResp` 和 `lastCmdResult`；空闲不打 `task_idle`。每回合另有一条 `event=round` 总览。

世界新闻日志标记为 `NEWS_INFER`：官方消息、传闻、LLM `promptText` 与输出 JSON 在发生时写入 stderr。无自进化 `phaseTask` 且本回合有推断活动、沙盒空闲时，经 `executeCmd` 的 `printf` 回传到下一回合 `lastCmdResult`。有自进化任务时不抢沙盒。字段说明见 [`docs/logging.md`](docs/logging.md)。

在平台运行结束后，从系统下载对局日志，搜索 `PIONEER_TASK`、`NEWS_INFER`、`read_document`、`execute_tool`、`submitAnswer`、`prompt` 或 `llmResp`。发出接取或提交指令不代表成功，应结合系统下一回合的动作结果、任务原文与错误核对。此日志回传不依赖下载选手容器中的本地 `logs/` 目录。

现有 docs 未提供平台上传、下载 API，也未说明下载文件包含哪些字段。因此本地已验证协议链路，实际平台 LLM、沙盒环境和最终下载文件可见性仍需上传对局验证。搜索限时 7 秒、工具执行限时 10 秒，单次工具输出最多保留 6000 字符；错误和截断信息会反馈给 LLM。

首日白天安全窗口保留先锋任务；临夜回防与夜间武器操控覆盖任务。解题器在防守时段不发出提交或沙盒指令，防止缓存响应重新占用守卫。
### 刷新方向与升级顺序（用户确认）

左上出生：机器人从右上刷新，优先右侧正面墙；右下出生：机器人从左下刷新，优先左侧正面墙。
按基地实际横坐标识别左右位置，不绑定challenger/defender，支持换边。
正面之后补前半部上下侧翼，不建后侧墙；这一筛选也用于第二天以后的补墙，旧计划中的后侧墙任务会取消。
方向已由用户确认，但具体合法建造格仍根据执行反馈校验。

道具任务顺序：补墙（缺口重建）> 武器升级券 > 修墙 > 围墙升级券 > 基地升级。
首日防线完成、且未到返程就位时间时，也可以执行升级任务。
存档中尚未购买道具的基地升级任务可重新分配给武器/围墙；已购基地券继续使用，避免浪费。
