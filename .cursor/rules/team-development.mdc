---
description: 全仓库多人协作、Python比赛接口和验证约定
alwaysApply: true
---

# 团队开发约定

- 按根目录 CONTRIBUTING.md 执行；改动前阅读相关代码与规则文档。
- 一个任务聚焦一个目标，保留用户已有改动，不混入无关重构或格式化。
- 多人修改 main.py 时先明确函数范围，公共接口变更明确输入输出和兼容性。
- 保持 POST /、callback 返回 dict、启动参数和比赛交付契约。
- 使用 Python 3.11.10 兼容语法；新增依赖固定版本并说明必要性。
- 不把未经验证的规则假设写成官方事实。
- 行为变更补充必要测试；运行 python -m unittest discover -s tests -v。
- 不提交密钥、运行日志、持久化状态、缓存或虚拟环境。
- 通过任务分支和 PR 交付，禁止擅自强推主线或丢弃他人改动。
- 报告修改结果、实际验证与限制，不将本地文件描述为已生效的远程设置。
